[training]
run_name = "mobilenetv2_cityscapes"

data_dir      = "data/downloads"
train_split   = "train"
val_split     = "val"

height = 512
width  = 1024
batch_size = 2
epochs = 20
learning_rate = 1e-4
augment = true
mixed_precision = false

# Modèle et loss
model_type = "mobilenetv2_deeplab_lite"
loss_type  = "ce_weighted"

# Poids de classes (7 classes)
class_weights = [1.0, 1.0, 1.5, 1.0, 1.0, 2.0, 2.0]

# Checkpoints : None => on garde tous les ckpts, k => Top-K
top_k_checkpoints = null

# Répertoires artefacts (le code ajoute automatiquement le suffixe run_name)
model_output_dir = "artifacts/models"
checkpoint_dir   = "artifacts/checkpoints"

# Suivi MLflow
mlflow_experiment = "cityscapes"

